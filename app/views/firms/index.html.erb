<p id="notice"><%= notice %></p>

<div class="container">
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Cuit</th>
        <th>Afip condition</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @firms.each do |firm| %>
        <tr>
          <td><%= firm.name %></td>
          <td><%= firm.cuit %></td>
          <td><%= firm.afip_condition %></td>
          <td><%= link_to 'Show', firm %></td>
          <td><%= link_to 'Edit', edit_firm_path(firm) %></td>
          <td><%= link_to 'Destroy', firm, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Firm', new_firm_path, class: 'btn btn-primary' %>
</div>
